import { SnowfallDirective } from './snowfall.directive';

describe('SnowfallDirective', () => {
  it('should create an instance', () => {
    const directive = new SnowfallDirective();
    expect(directive).toBeTruthy();
  });
});
